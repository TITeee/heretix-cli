; Behaviour settings only — no registry is declared here, so every package
; still resolves from the public registry and no scope can be substituted.
; Taken from a real project where the mere presence of this file caused every
; scope missing from the built-in well-known list to be reported.
shamefully-hoist=true
confirm-module-purge=false
